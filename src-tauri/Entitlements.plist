<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	Without these, the hardened runtime our codesign step applies blocks
	WebKit's JavaScriptCore JIT from allocating executable memory, which
	crashes the WKWebView WebContent process ("web content process
	terminated") under any sustained JS/canvas workload — reliably hit by
	the editor's canvas work, but not by `npm run tauri dev` since that
	binary isn't hardened-runtime signed at all.
	-->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.allow-dyld-environment-variables</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
</dict>
</plist>
